# FFDL SCSS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Copy FFDL’s SCSS into `frontend/assets/scss` so Weekly Likes compiles the framework (minus demo files) through the existing Nuxt CSS entry.

**Architecture:** Vendor a one-time copy of `frontend-framework-dirk-lach/src/scss` into the Nuxt app. Weekly Likes owns the files. Strip demo-only forwards. Keep `nuxt.config.ts` pointing at `~~/assets/scss/main.scss`.

**Tech Stack:** Nuxt 4, Sass (`sass` already in `frontend/package.json`), FFDL SCSS (`@use` / `@forward`).

## Global Constraints

- Copy from `/Users/dirklach/Documents/Webentwicklung/dirklach/frontend-framework-dirk-lach/src/scss` only (no demo HTML/JS, no compiled CSS, no framework tooling).
- Do not change `_config.scss` tokens.
- Do not change `app.vue`, `nuxt.config.ts`, or add packages.
- Exclude `7-utilities/_demo.scss` and `6-components/_footer.scss`.
- `6-components/_index.scss` forwards nothing.
- `7-utilities/_index.scss` forwards `margin` only.

---

### Task 1: Replace stub SCSS with FFDL (minus demo)

**Files:**
- Delete: `frontend/assets/scss/_reset.scss`
- Delete: `frontend/assets/scss/_space.scss`
- Create: `frontend/assets/scss/main.scss` (overwrite)
- Create: `frontend/assets/scss/1-variables/_config.scss`
- Create: `frontend/assets/scss/1-variables/_index.scss`
- Create: `frontend/assets/scss/2-tools/_breakpoints.scss`
- Create: `frontend/assets/scss/2-tools/_index.scss`
- Create: `frontend/assets/scss/3-generic/_reset.scss`
- Create: `frontend/assets/scss/3-generic/_global.scss`
- Create: `frontend/assets/scss/3-generic/_index.scss`
- Create: `frontend/assets/scss/4-layout/_grid.scss`
- Create: `frontend/assets/scss/4-layout/_col.scss`
- Create: `frontend/assets/scss/4-layout/_subgrid.scss`
- Create: `frontend/assets/scss/4-layout/_section.scss`
- Create: `frontend/assets/scss/4-layout/_index.scss`
- Create: `frontend/assets/scss/5-styles/_text-headings.scss`
- Create: `frontend/assets/scss/5-styles/_text-copy.scss`
- Create: `frontend/assets/scss/5-styles/_text-additional.scss`
- Create: `frontend/assets/scss/5-styles/_index.scss`
- Create: `frontend/assets/scss/6-components/_index.scss`
- Create: `frontend/assets/scss/7-utilities/_margin.scss`
- Create: `frontend/assets/scss/7-utilities/_index.scss`
- Create: `frontend/assets/scss/8-themes/_index.scss`
- Test: compile `frontend/assets/scss/main.scss` with `sass`

**Interfaces:**
- Consumes: FFDL source tree at `/Users/dirklach/Documents/Webentwicklung/dirklach/frontend-framework-dirk-lach/src/scss`
- Produces: Nuxt CSS entry `frontend/assets/scss/main.scss` that `@use`s layers `1-variables` through `8-themes`

- [ ] **Step 1: Compile current stub and capture that `.grid` is missing**

Run from `frontend`:

```bash
pnpm exec sass assets/scss/main.scss --no-source-map | rg "\.grid"
```

Expected: no `.grid` rule (stub has only reset, space tokens, and `body`).

- [ ] **Step 2: Copy FFDL SCSS, then strip demo files**

```bash
SRC="/Users/dirklach/Documents/Webentwicklung/dirklach/frontend-framework-dirk-lach/src/scss"
DEST="/Users/dirklach/Documents/Webentwicklung/dirklach/weekly-likes/frontend/assets/scss"

rm -f "$DEST/_reset.scss" "$DEST/_space.scss"
rsync -a --delete --exclude '_demo.scss' --exclude '_footer.scss' "$SRC/" "$DEST/"
```

Write `frontend/assets/scss/6-components/_index.scss`:

```scss
// ==================================================================
// Components / Index
// ==================================================================
```

Write `frontend/assets/scss/7-utilities/_index.scss`:

```scss
// ==================================================================
// Utilities / Index
// ==================================================================

@forward "margin";
```

`main.scss` must be:

```scss
@use "1-variables" as *;
@use "2-tools" as *;
@use "3-generic" as *;
@use "4-layout" as *;
@use "5-styles" as *;
@use "6-components" as *;
@use "7-utilities" as *;
@use "8-themes" as *;
```

- [ ] **Step 3: Compile and confirm grid + no demo/footer**

```bash
cd /Users/dirklach/Documents/Webentwicklung/dirklach/weekly-likes/frontend
pnpm exec sass assets/scss/main.scss /tmp/ffdl-weekly-likes.css --no-source-map
```

Expected: exit 0.

Then:

```bash
rg -n "^\.grid \{|^body\.demo|^\.c-footer \{|--space-2xs" /tmp/ffdl-weekly-likes.css
```

Expected: a `.grid {` rule; no `body.demo`; no `.c-footer {`; no `--space-2xs` (old stub token).

- [ ] **Step 4: Confirm Nuxt still points at the same CSS entry**

`frontend/nuxt.config.ts` must still contain:

```ts
css: ["~~/assets/scss/main.scss"],
```

Do not edit this file unless that line is missing.

- [ ] **Step 5: Commit** (only if the user asked)

```bash
git add docs/superpowers/specs/2026-09-04-ffdl-scss-design.md docs/superpowers/plans/2026-09-04-ffdl-scss.md frontend/assets/scss
git commit -m "$(cat <<'EOF'
Add FFDL SCSS as the Nuxt frontend foundation.

EOF
)"
```
